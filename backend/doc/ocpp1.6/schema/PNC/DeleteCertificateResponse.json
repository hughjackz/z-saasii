{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DeleteCertificateConf",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["Accepted", "Failed", "NotFound"],
      "description": "Required. Charge Point indicates if it can process the request."
    }
  },
  "required": ["status"],
  "additionalProperties": false
}