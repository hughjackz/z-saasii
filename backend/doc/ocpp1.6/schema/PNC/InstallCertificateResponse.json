{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "InstallCertificateConf",
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "enum": ["Accepted", "Rejected", "Failed"],
      "description": "Required. Charge Point indicates if installation was successful."
    }
  },
  "required": ["status"],
  "additionalProperties": false
}