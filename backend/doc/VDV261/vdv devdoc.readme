VDV261充电桩后台开发功能项清单
1　背景知识
    VDV261充电桩后台主要是对VDV261协议涉及的EVCC与VDV261后台的交互做处理。并提供VDV261相关的配置项控制。
2　消息交互
    VDV261的消息分为请求与应答。
    请求为EVCC上报消息给后台，
    应答为后台下发配置给EVCC。
    2.1　消息类型
        HTTPPOST
    2.2　请求消息
        URL：https://vdvtest.softenv.com/vdv
        内容类型：json 请求参数如下 见backend/doc/VDV261/schema/VDV261req.json
        VDV261.req
        |字段			|名称                   |类型	
        |seq			|序号                   |int	
        |vin			|车辆VIN				|String	
        |evccid			|EVCCID					|String	
        |odo			|里程（km）	            |int	0~21055406
        |bat_reqtime	|高压电池预处理所需时间	|int	0~250
        |bat_eamount	|高压电池充电所需能量	|int	0~1000
        |prec_eamount	|车辆预处理所需能量		|int	0~1000
        |prec_reqtime	|车辆预处理所需时间		|int	0~250
        |chrg_stat		|车辆充电状态			|int	0，ready to charge
        |				|						|		1,charging
        |				|						|		2,charging impossible
        |data_num		|数据序号				|String	
        通过请求消息，EVCC将自身的vin、充电信息状态进行上报。

    2.3　应答消息
        内容类型：json   应答参数如下 见backend/doc/VDV261/schema/VDV261resp.json
        |VDV261.resp
        |字段		|名称				|类型	
        |seq		|序号				|int	
        |vin		|VIN				|String	
        |driveoff	|预定出发时间		|int	0~1440
        |prec_dsrd	|DMS预处理请求		|int	0，no
        |								|		1，yes
        |prec_hvac	|HVAC预处理请求		|int	0，only hot water and ventilation
        |			|					|		1,hot water and heating
        |			|					|		2,no hot water, cooling or ventilation
        |			|					|		3,no preconditioning or SNA
        |ambienttemp|环境温度			|int	-50~70

    2.4　HTTP登录
        EVCC通过VDV261与后台交互建立前，需要通过登录动作，完成自身的授权认证。对于HTTP头部不包含认证信息的，服务器应给与401未授权反馈。
        因此常用的做法是，在VDV261充电桩后台，以Interceptor的形式加入拦截器。用于在连接建立前，拦截登录请求，判断授权信息，并对无授权信息的请求进行拦截。
        因此HTTP头部携带的参数，应包含授权信息的内容，授权应实现HTTP协议中的Basic认证。头部参数说明如表3所示。
        |-------------------------------------------------------------------------------------
        |字段名         |含义	        |说明	                          |值样例
        |-------------------------------------------------------------------------------------
        |authorization  |授权token	    |采用Basic认证方式组合的身份信息	|Basic XXX
        |-------------------------------------------------------------------------------------
        Basic为固定字符串，后接1个空格字符，再后XXX为Base64编码的身份信息。即base64(vin:password)。
   
    2.5　车辆http鉴权说明
        EVCC会将身份信息组合，并填入authorization头部。对于VDV261后台来说，身份信息为：
        a)	vin，车辆的VIN字符串；
        b)	password,应注册到VDV261后台时，后台为其设置的密码。
        身份信息会组合成一个字符串，形式为vin:password。例如：vin为camaevcc01，password为123456。那么身份信息应为：camaevcc01:123456。
        在拦截到VDV261登录时，做如下判断操作：
        a)	判断authorization是否正确，如不正确，登录失败，响应401；
        b)	（优化）判断vin及密码是否在数据库表中，如不在，登录失败，响应401；

    2.6　车辆信息处理
        建立两张数据库表格，
        1.VDVProfile 数据包含id、name、request中的driveoff、prec_dsrd、prec_hvac、ambienttemp，新建该表时，新建一行默认的数据id[uuid]、name[default]、request中的driveoff[480]、prec_dsrd[0]、prec_hvac[0]、ambienttemp[25]
        2.VDVCarInfo 数据包含id、车辆VIN、初始密码为：123456、EVCCID(车辆上传报文时更新)、odo(车辆上传报文时更新)，VDVProfile_id，所属的CP_OP


3　组网配置说明，见backend/doc/VDV261/network toplogy.png 
    整体上链路通讯需要两组通道，即图中实线标明的为物理链接，图中虚线标明的为EVCC使用SECC提供的Internet服务上网后，进行的访问通讯。需要关注的点如下：
    a) SECC与EVCC采用PLC通讯，这中间采用IPv6的Link-Local地址进行交互。（不属于本项目工作）
    b) EVCC使用SECC提供的Internet服务进行网络访问。（不属于本项目工作）
    c) 路由器需要配置有RDNSS及SLAAC，可以给新的链接提供IPv6地址，可以采用邻居发现提供DNS服务器的地址。（不属于本项目工作）
    d) DNS服务器中配置了正向地址及反向地址，将测试服务器Back-End的域名配置为vdvtest.softenv.com，指向2001:0da8:1005:1000::101。并配置好反向指针。
    e) 测试服务器中需要安装V2ICP根证书（与提供给EVCC内安装的一致），并提供HTTPS服务。

4　功能项清单
    后台需具备的功能项清单，如表4所示。
    -----------------------------------------------------------------------------------------------------------
    |序号	|功能项	                     |说明
    |-----------------------------------------------------------------------------------------------------------
    |1	    |基础B/S或C/S互联网后台架构	  |应拥有一个后台服务器，可处理HTTPS请求。
    |------------------------------------------------------------------------------------------------------------
    |2	    |VDV261证书+V2ICP证书，      |目前我们使用自制证书。如果需要CA认证，可能需要到证书签发机构获取相关证书。
    |------------------------------------------------------------------------------------------------------------
    |3	    |VDV261消息请求处理功能	     |能接收json格式的消息，并解析相关数据。
    |------------------------------------------------------------------------------------------------------------
    |4	    |VDV261消息应答功能	         |能根据配置，对VDV261的请求消息进行应答。
    |------------------------------------------------------------------------------------------------------------
    |5	    |结合业务相关的页面逻辑	      |结合已有框架，具体实现对VDV261数据的处理业务逻辑，并根据需要配置修改VDV261消息应答。
    |------------------------------------------------------------------------------------------------------------
    |6       前端功能可从车联网批量导入vin至该表中，并自动插入初始密码。
    EVCC定期将充电过程中的相关数据通过互联网，请求VDV261后台。VDV261后台可实时接收车辆充电信息，并可通过后台对VDV261应答消息中的参数进行配置，达到远程下发配置的目的。
6　自签名证书
    对于VDV261需要使用的自签名证书，可以采用多种方式制作。先前我们的经验是使用OpenSSL制作自签名的V2ICP证书。包含私钥及证书。
    曲线：secp256r1(openssl里称为prime256v1)
    哈希：SHA256
    注：与ISO15118-2的证书配置一致，都是ECDSA-with-SHA256。
    该自签名证书需要放置到VDV261后台中，在建立TLS连接通信时，根据后台的配置，出具此证书。此证书需要EVCC验证，判断是否与EVCC内存储的V2ICP根证书是同一个根证书。
